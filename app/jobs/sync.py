# Your existing code

# Update the f-string detail line

# other code remains unchanged
detail = f"new_issues_processed={processed}, skipped_seen={skipped_seen}, budget_remaining={budget.remaining}"

# remaining code